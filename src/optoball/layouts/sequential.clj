(ns optoball.layouts.sequential
  (:require [optoball.models.time :as time]))

;; (defn layout [slots teams]
;;   (let [slots-by-week (group-by (comp t/week :time) slots)]
;;     teams-by-division (group-by :division teams)
;;     )
;;   )